Объявление класса:

```javascript
class User {
  constructor(name) {
    this.name = name;
  }

  sayHi() {
    alert(this.name);
  }

}
```

Создание объекта:

```javascript
let user = new User("Иван");
user.sayHi();
```

